#include <iostream>
#include <algorithm>
#include <cstring>
using namespace std;

// 全局数组——大小按题面数据范围来定
// const int MAXN = 105;
// int dp[MAXN], w[MAXN], v[MAXN], val[MAXN];

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // 读数据 + 算答案

    cout << ans << '\n';
    return 0;
}
